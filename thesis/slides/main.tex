% Make dvipsnames colours (SeaGreen, RoyalBlue, ...) used by shared thesis
% figures available; must precede beamer's own xcolor load to avoid a clash.
\PassOptionsToPackage{dvipsnames}{xcolor}
\documentclass[aspectratio=169]{beamer}

% Choose how your presentation looks.
%
% For more themes, color themes and font themes, see:
% http://deic.uab.es/~iblanes/beamer_gallery/index_by_theme.html
%
% ---- Frame numbering: the talk proper counts 1..N, and the supplementary
% part restarts at i so the backup slides do not inflate the talk's count.
% Both totals are recorded in the .aux at the point where they are known and
% are available from the *next* run on (latexmk reruns until stable); until
% then the fallbacks below keep the footline sane.
\newif\ifsupplementary
\providecommand{\mainframetotal}{\inserttotalframenumber}
\providecommand{\suppframetotal}{0}
\makeatletter
\newcommand{\recordmainframetotal}{%
  \immediate\write\@auxout{\string\gdef\string\mainframetotal{\the\value{framenumber}}}}
\newcommand{\recordsuppframetotal}{%
  \immediate\write\@auxout{\string\gdef\string\suppframetotal{\the\value{framenumber}}}}
\makeatother
% Kept in its own macro: \setbeamertemplate cannot take a bare \if...\fi.
\newcommand{\framecountline}{%
  \ifsupplementary
    \romannumeral\value{framenumber}\relax{} / \romannumeral\suppframetotal\relax
  \else
    \insertframenumber{} / \mainframetotal
  \fi}

% ---- Source marker: \source{key,...} anywhere inside a frame prints
% "Source: [n]" in the lower-left corner (the free half of the footline) and
% puts the entry into the bibliography, exactly as \cite would.  The keys are
% stored together with the frame number they were set on; the footline prints
% them while that number is current -- so the mark repeats on every overlay of
% its own frame -- and drops them again as soon as it is not, which is why no
% per-frame reset hook is needed and why the supplementary part restarting the
% counter at 0 cannot resurrect a mark from the talk proper.
\newcommand{\sourcekeys}{}
\newcommand{\sourceframe}{-1}
\newcommand{\source}[1]{%
  \gdef\sourcekeys{#1}%
  \xdef\sourceframe{\the\value{framenumber}}}
\newcommand{\sourcemarkline}{%
  \ifnum\sourceframe=\value{framenumber}\relax
    {\scriptsize\color{black!60}Source: \cite{\sourcekeys}}%
  \else
    \gdef\sourcekeys{}\gdef\sourceframe{-1}%
  \fi}

\mode<presentation>
{
  \usetheme{Darmstadt}
  \usecolortheme{beaver} % or try albatross, beaver, crane, ...
  \usefonttheme{serif}  % or try serif, structurebold, ...
  % Darmstadt (smoothbars) gives the subsection its own bar under the section
  % strip.  Nothing fills it any more -- the backup structure was promoted from
  % subsections to sections so that it shows up in the section strip itself --
  % so the bar is an empty rule on every slide.  Keep the background light and
  % the foreground at full-strength structure red: light so the empty strip
  % stays quiet, red in case a subsection is ever reintroduced.
  \setbeamerfont{subsection in head/foot}{size=\small,series=\bfseries}
  \setbeamercolor{subsection in head/foot}{fg=darkred,bg=gray!18!white}
  \setbeamertemplate{navigation symbols}{}
  \setbeamertemplate{caption}[numbered]
  \setbeamertemplate{footline}{
    \hspace{2em}\sourcemarkline\hfill\framecountline\hspace{2em}\vspace{2mm}
  }
}

\usepackage[english]{babel}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}   % scalable TS1 fonts (as in report/main.tex); LISC has no
                       % METAFONT, so bitmap tcrm* cannot be generated on demand
\usepackage{minted}
\usepackage{tikz}
\usepackage{tikz-cd}
\usepackage{pgfplots}          % axis/addplot in the cosine_vs_wasserstein_{c,d} panels
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta,positioning,fit,calc,shapes.multipart,shadows.blur,bending}
% extra libraries required by the shared thesis figures:
\usetikzlibrary{backgrounds,patterns,angles,quotes,shapes.geometric,shapes.symbols}
\usetikzlibrary{shapes.misc}   % 'rounded rectangle' nodes in the ms_architecture_* figures
\usetikzlibrary{decorations.pathreplacing,decorations.pathmorphing,decorations.markings}
\usepackage{graphicx}
\usepackage{adjustbox}                                % fit figures within width AND height
\usepackage{import}                                   % robust relative paths for shared snippets
\graphicspath{{./}{../shared/figures/}}     % own dir first, then shared figures
\usepackage{subcaption}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{amsmath}
\usepackage{chemfig}
\usepackage{amssymb} % for \longrightarrow in math, optional
\usepackage{textcomp} % optional
\usepackage{csquotes} % recommended by biblatex

\usepackage[backend=biber,style=numeric,sorting=none]{biblatex}
\addbibresource{../shared/refs.bib}   % shared bibliography (was references.bib)
% The References slide is a single 90mm-tall beamer frame: at the default
% \bibfont and item spacing the list runs off the bottom once more than a
% handful of works are cited (a bare \scriptsize before \printbibliography
% has no effect -- biblatex selects its own font).
\renewcommand*{\bibfont}{\tiny}
\setlength{\bibitemsep}{1pt}

% ---- Shared definitions (notation + figure style), see shared/notation.tex
\input{../shared/notation}

\newcommand{\checkline}[1]{{\footnotesize\color{black!70}#1}}

% ---- Figures: embed the PDF if it exists, else show a red placeholder box.
% Mirrors report/main.tex; no inkscape on LISC, so svg conversion is not an option.
\newcommand{\ph}[1]{\textcolor{red}{\textbf{#1}}}
% Restores the report's text metrics inside a shared figure: 12pt base with
% \baselinestretch 1.5 (see report/frontmatter.tex). Several figures tune
% multi-line node labels with a negative \\ offset (\\[-10pt], \\[-0.7em]),
% which collapses into overlapping lines at beamer's much smaller
% baselineskip. \linespread rather than a fixed baselineskip, so nodes that
% select their own size (\small, \scriptsize) are covered too. Use inside a
% box that scales the picture to fit -- only the ratio matters, not the
% absolute size.
\newcommand{\reportmetrics}{\linespread{1.5}\fontsize{12}{14.5}\selectfont}
% The optional first argument caps the height: a 16:9 frame is shorter than
% the old 4:3 one, so a figure sized only by \linewidth can outgrow the frame.
\newcommand{\figorpending}[3][0.68\textheight]{%
    \IfFileExists{#3}{\adjustbox{max totalheight=#1}{\includegraphics[#2]{#3}}}{%
        \framebox[0.8\linewidth]{%
            \rule{0pt}{0.3\textheight}%
            \ph{[figure pending]}%
        }%
    }%
}


%\setbeameroption{show notes}
% Rehearsal build: notes beside the slide on a double-width (320x90mm) page,
% for a presenter tool that splits it across two screens.  Defining \nonotes
% before this file is read (see main-nonotes.tex) gives the clean 160x90mm
% projector deck instead.
\ifdefined\nonotes
  \setbeameroption{hide notes}
\else
  \setbeameroption{show notes on second screen=right}
\fi



\lstset{
    language=Python,
    basicstyle=\ttfamily\scriptsize,
    tabsize=1,
    keywordstyle=\color{blue},
    commentstyle=\color{gray},
    stringstyle=\color{green!60!black},
    showstringspaces=false,
    frame=single,
    breaklines=true
    }

\title[MS-Thesis]{Bidirectional Mass Spectrometry Modeling Aided by Auxiliary Intermediate Steps From Graph Transformation Tools}
\author{Philipp Reiser}
\institute{Institut für Theoretische Chemie\\Theoretical Biochemistry Group\\Supervisor:\\ao. Univ.-Prof. Mag. Dr. Christoph Flamm}
\date{2026-08-27}

\begin{document}

\begin{frame}
  \titlepage
\end{frame}
\note{
    Welcome, everyone, and thank you for coming --- a particular welcome to the chair and to the members of the committee.
    My name is Philipp Reiser, and I will be presenting my master's thesis, carried out in the Theoretical Biochemistry Group under the supervision of Christoph Flamm.
}

\begin{frame}{Outline}
  \tableofcontents
\end{frame}
\note{
    Let me begin with a brief outline of the talk.

    I will start with the problem statement and the background on mass spectrometry.
    I then turn to the data, and to how synthetic data is generated.
    This is followed by the machine-learning model and the results.
    I will close with the conclusions I draw from the work.
}

\section{Problem}

\begin{frame}{Motivation}
    \centering
    \resizebox{0.95\textwidth}{!}{
        \input{../shared/figures/motivation.tikz.tex}
    }
    \vskip 0.6cm

    \begin{itemize}
        \item Mass spectrometry is widely used, but analysis is the challenge
        \item Data acquisition is fast, interpretation slow $\Rightarrow$ the bottleneck
    \end{itemize}

\end{frame}
\note{
    What is the motivation?

    Mass spectrometry is among the most widely used techniques for analyzing molecules.
    Interpretation of the data for novel molecules, however, is still largely manual and expert-driven, and correspondingly slow.

    Data acquisition is fast --- and none of that speed carries over into the analysis.
    That asymmetry is where the bottleneck sits.

    Therefore, developing computational methods in structure elucidation is a worthwhile goal.
}

\begin{frame}{Problem Statement}
    \centering
    \begin{adjustbox}{max totalsize={0.98\textwidth}{0.76\textheight},center}
        \input{../shared/figures/problem_statement.tikz.tex}
    \end{adjustbox}
    \vskip 0.2em
    {\footnotesize Structure elucidation is the inverse of a map that is not invertible.}
\end{frame}
\note{
    Stated formally, the problem is a map between two spaces.
    On the left, the space of molecular structures. On the right, the space of mass spectra.

    Under fixed measurement conditions, the forward direction is well behaved:
    one structure gives one spectrum.

    What I am ultimately after is the other direction, so-called structure elucidation.
    Given a spectrum, recover the structure.

    But that map is not invertible.

    The shaded region is everything within epsilon of the measured spectrum.
    Several different molecules land inside it: their spectra do differ, but by less than the tolerance.

    Here the inverse is the red arrow.
    What matches a given spectrum, within epsilon, is not one molecule. It is a whole set of candidates.
}

\section{Background}

\begin{frame}{Mass Spectrometer}
    \centering
    \begin{adjustbox}{max totalsize={0.95\textwidth}{0.74\textheight},center}
        \input{../shared/figures/ms-schematic.tikz.tex}
    \end{adjustbox}
\end{frame}
\note{
    A brief reminder of the measurement principle.

    We start with a sample, which is first ionized and accellerated into the mass analyzer.

    There the ions are then separated according to their mass-to-charge ratio, and are finally registered at a detector --- which is what yields the spectrum.

    The point that matters for this work:
    under electron ionization the molecule does not remain intact, but fragments.
    The spectrum is therefore a fingerprint of those fragments, and it is precisely that structure I exploit later on.
    So the spectrum can be related to certain molecular structures.
}

\begin{frame}{Modelling Mass Spectra -- An Open Problem}
    \begin{itemize}
        \item \textbf{Rule-based} but limited to rule set
        \item \textbf{Data-driven} models do not generalize
        \item \textbf{Ab initio} does not scale
        \item currently ${<}20\%$ of detected compounds are identified
    \end{itemize}
    \source{Young2024,Nowatzky2025,Hamaneh2024,Blazenovic2018}
\end{frame}
\note{
    Before I continue, one point on the state of the art.

    Modelling mass spectra is not a solved problem.

    There are three families of approaches, and each trades something away.
    Rule-based in silico fragmentation is interpretable, but it only ever sees what the rule set encodes.
    Data-driven models do well on molecules that resemble their training data, but they carry no mechanism and do not generalize on unseen structures.
    Ab initio calculations are mechanistically faithful, but far too expensive to run at a big scale.

    So in practice four out of five compounds an instrument detects are never named --- that is the size of the problem, and why even a partial improvement is worth having.

    So this is open ground --- which is what motivates the route I take next.
}

\begin{frame}[fragile]{M\O D -- A Graph Transformation Tool}
    \begin{columns}[T,onlytextwidth]
      \column{0.52\textwidth}
      \begin{itemize}
        \item Molecules as labelled graphs, reactions as \emph{rewrite rules}
        \item Repeated application builds the \emph{derivation graph} of a reaction network
        \item A rule is a span \(L \leftarrow K \rightarrow R\):
          \begin{itemize}
            \item[] \(L\) -- what must be present
            \item[] \(K\) -- what is preserved (context)
            \item[] \(R\) -- what it becomes
          \end{itemize}
      \end{itemize}

      \column{0.44\textwidth}
      \centering
      \begin{tikzcd}[column sep=large, row sep=large]
      L \arrow[d, "m"']
      & K \arrow[l, "l"'] \arrow[r, "r"] \arrow[d]
      & R \arrow[d, "m^{*}"] \\
      G
      & D \arrow[l, "l^{*}"] \arrow[r, "r^{*}"']
      & H
      \end{tikzcd}

      \vspace{0.6em}
      {\footnotesize Host graph \(G\) $\rightarrow$ product graph \(H\)}
    \end{columns}
    \source{Andersen2017,MODDocDGStrat}
\end{frame}
\note{
    \scriptsize
    All of the fragmentation chemistry in this work is expressed as graph transformation, and the tool that carries it out is MÖL.
    MÖL was developed, in the Theoretical Biochemistry Group --- among its authors is my supervisor, Christoph Flamm.

    In one sentence: a molecule is a labelled graph, a reaction is a rule that rewrites part of that graph, and applying rules over and over gives the derivation graph --- the network of everything reachable from the starting molecule.

    A rule has three parts: the left-hand side is the pattern that must be present, the interface is the context that is kept, and the right-hand side is what it becomes.


    And that is the end of the background.
    Everything from here on --- the rule encoding, the predicates, the data generation, the model and the evaluation --- is my own work.
}


\section{Data}
\begin{frame}{Data Sources}
    \begin{itemize}
        \item Mass spectrometry data from NIST $\rightarrow$ SMILES strings \& spectra
        \item Curated EI fragmentation reactions $\rightarrow$ encoded Graph Transformations
    \end{itemize}

	\begin{columns}[T,onlytextwidth]
	  \column{0.45\textwidth}
	  \centering
	  \figorpending{width=\linewidth}{../shared/figures/TolueneFragmentation.pdf}

	  \column{0.45\textwidth}
	  \centering
	  \includegraphics[width=\linewidth]{ms_spectra_toluene.PNG}
	\end{columns}
	% left panel: fragmentation scheme, right panel: reference spectrum
	\source{src-wiki-toluene-fragmentation,src-wiki-toluene-spectra}
\end{frame}
\note{
    \scriptsize
    Two data sources enter the work.

    The first is mass-spectrometry data from NIST.
    It is considerd the gold standard reference library for electron-ionization spectra.
    It provides the structures, as SMILES strings, together with the measured spectra.

    %One thing to note about NIST, it is a reference library, not a cleaned training set: its spectra contain peaks that no fragment of the molecule can account for --- isotopes, and background.
    %I did not filter those, so part of what my model is scored against is not explainable even in principle.
    %Keeping only peaks whose formula is a sub-formula of the precursor is the obvious fix.

    The second source is a set of curated EI fragmentation reactions from the literature, which I have encoded as Graph Transformations.
    On the left is the fragmentation of toluene, on the right the corresponding spectrum. I have only encoded 153 reactions, so the coverage is limited.

    The corpus is only 66 molecules, due to the high computational cost of my unoptimized code.
}


\begin{frame}{Data Generation Overview}
    \centering
    \begin{adjustbox}{max totalsize={0.75\textwidth}{0.82\textheight},center}
        \import{../shared/figures/}{dataGenOverview.tikz.tex}
    \end{adjustbox}
\end{frame}
\note{
    \scriptsize
    This figure summarizes the entire pipeline of the synthetic data generation.

    A molecule is fragmented by applying the rule set in a Graph Transformation Tool.

    That yields a fragmentation graph, from which the fragments are read off.

    The reverse direction then \textbf{recombines} those fragments into theoretically possible structures, using the inverted reaction rules.

    This is how candidate structures for a given spectrum are obtained.

%    Now the practical part, because it is what capped the corpus: this expansion is expensive, and I profiled where the time goes.
%    MÖL searches every match of a rule's left-hand side, and for each of those it also enumerated every mapping of the right-hand side.
%    On a symmetric molecule that product runs into the thousands, and every one of them called back into my Python predicate.
%    On 2-propanol that was half a million traversals for eleven species, and in a sampled profile eighty-six percent of the time sits inside MÖL's C++ matching loop --- so this was never a matter of tuning my own code.
%
%    In numbers from April: catechol took sixty-four hours, and the hardest molecules exceeded their wall-clock limit and silently dropped out of the corpus.
%    That is why the corpus is 66 and not 6000.
%    Three changes --- reading charge and mass off the graph labels, memoising the predicate on the rule's own positions, and capping the redundant right-side multiplicity --- bring catechol under a second, on byte-identical dumps.
%    So it is optimization, not skipped work; the measurements are in the backup slides.
}

\begin{frame}[fragile]{Sample Fragmentation Rules}
    \begin{figure}
        \centering
        \resizebox{0.85\linewidth}{!}{\input{../shared/figures/IMS_book_page_57.tikz.tex}}
    \end{figure}
    \source{McLaffertyFredW1995IvM}
    \vspace{0.7cm}
	\begin{minted}[
	  linenos,
	  numbersep=6pt,
	  fontsize=\scriptsize,
	  breaklines,
	  frame=single
	]{python}
	IMS_4_9 = mod.Rule.fromDFS(
	    s =
	    "[C]1[C]2([C]3)([C]4)[_A+.]5"
	    ">>"
	    "[C.]1" "." "[C]2([C]3)([C]4){=}[_A+]5",
	    name =
	    "radical induced (alpha-)clevage for a saturated site"
	    " 4.9"
	    " §R1R3R4Y5"
	)
	\end{minted}
\end{frame}

\note{
    At the top you see one of these rules in its original, literature form.

    The essential observation is that the formulation is not restricted to individual atoms.
    It also quantifies over entire substituent groups.
    Those are the R and Y placeholders, for alkyl and heteroatom groups.

    Below is the same rule in code.
    The graph transformation itself operates on single atoms only.
    So I carry the group conditions in the rule name, after the paragraph sign.
    Here atoms 1, 3 and 4 are required to be alkyl groups, and atom 5 a heteroatom group.

    %One atom misplaced on the left-hand side of a rule --- a single character --- and the expansion matches everywhere, recombines, and explodes.
    %Some of my month-long runs were exactly that, and this picture is where it had to be found.

    On the following slides you will see how this notation is enforced during the transformation.
}


\begin{frame}{Predicate}
    \centering
    \resizebox{0.95\textwidth}{!}{
        \input{../shared/figures/predicate-filter.tikz.tex}
    }
\end{frame}
\note{
    MÖL provides an interface for so-called predicates.
    Predicates are callbacks that are evaluated during the transformation.
    They are here used to filter candidate matches according to the rule extension.
    The next few slides discuss the ones I implemented.
}

\begin{frame}{Predicate -- Alkyl Group}
    \centering
    \resizebox{0.95\textwidth}{!}{%
        \input{../shared/figures/predicate-alkyl.tikz.tex}
    }
\end{frame}
\note{
    The first is the alkyl group.
    An alkyl substituent consists of nothing but carbon and hydrogen.

    I start at the atom at which the rule is defined.
    From there I collect the neighborhood breadth-first.
    The search terminates at atoms that already belong to the match of the original rule.

    The condition is then simply whether every collected atom is a carbon or a hydrogen.
    If so, the group is accepted as alkyl; otherwise the match is rejected.
}

\begin{frame}{Predicate -- Heteroatom Group}
    \centering
    \resizebox{0.95\textwidth}{!}{%
        \input{../shared/figures/predicate-heteroatom.tikz.tex}
    }
\end{frame}
\note{
    The heteroatom group is the complementary condition.
    Here at least one atom is required to be \emph{neither} carbon \emph{nor} hydrogen.

    Again I collect the neighborhood breadth-first.
    Only atoms the original rule has not matched are considered.

    The set is then tested for any element outside hydrogen and carbon.

    If such an atom is present, the group is accepted.
    If the set is hydrogen and carbon only, the match is rejected.
}

\begin{frame}{Predicate -- Saturation}
    \vspace{0.5em}
    \centering
    \begin{adjustbox}{max totalsize={\textwidth}{0.78\textheight},center}
        \input{../shared/figures/predicate-saturation.tikz.tex}
    \end{adjustbox}
\end{frame}
\note{
    The third predicate is saturation.
    The requirement is a chain of carbon atoms connected exclusively by single bonds, in which every carbon carries hydrogen substituents only.

    The search proceeds from the start atom to the end atom, along carbon--carbon single bonds.
    The upper example satisfies the condition.
    The lower example does not, because of the branched carbon.
}

\begin{frame}{Sample Fragmentation Graph}
    \begin{figure}
        \centering
        \adjustbox{max totalheight=0.8\textheight}{\includegraphics[width=0.7\linewidth]{dg_toluene.pdf}}
    \end{figure}
\end{frame}
\note{
    This is what the fragmentation produces, drawn as a hypergraph.

    The vertices are the fragments, and the edges are the reactions relating them.
    The content of the individual nodes is not meant to be read here.

    The relevant point for the talk is the structure: a single input molecule gives rise to a connected space of fragments, and that space is what the model gets to work with.
}

\section{ML-Modeling}

\begin{frame}<1-5>{Bidirectional Framework\only<5>{ -- at Query Time}}
    \centering
    \IfFileExists{../shared/figures/bidirectional_ring.tikz.tex}{%
        \begin{adjustbox}{max totalsize={\textwidth}{0.82\textheight},center}
            \input{../shared/figures/bidirectional_ring.tikz.tex}
        \end{adjustbox}%
    }{%
        \framebox[0.8\linewidth]{%
            \rule{0pt}{0.3\textheight}%
            \ph{[figure pending]}%
        }%
    }

    \note<1>{
        Here is the problem slide again: molecules on the left, spectra on the right.
        For each molecule I keep two views side by side --- its molecule graph, and
        the fragmentation graph the derivation produces. On the right, the measured
        spectrum.
    }
    \note<2>{
        What's new is the middle --- one shared latent space that both sides are encoded into. Both encoders map straight in: the molecule graph, the fragmentation graph, and the spectrum.
        The dots are the index: every known molecule already has a place in here.
        Highlighted is one matched pair --- a molecule's latent and its own spectrum's latent.
    }
    \note<3>{
        Going clockwise over the top is Phase A, the forward direction.
        The decoder does not emit a free-form spectrum, it predicts one intensity per fragment.
        So every peak sits at a chemically realisable mass by construction.
        And it is the same decoder either way --- fed a molecule's own latent here in Phase A, or a spectrum's latent in Phase B.
    }
    \note<4>{
        Coming back underneath is Phase B, the backward direction: nearest-neighbour retrieval over that same molecule index.
        What comes back is a \emph{ranked set} of known structures, not a generated molecule.
        That green tie in the middle is InfoNCE.
        During training it pulls each spectrum latent onto its own molecule.
        And it pushes away from the others in the batch, which is what makes the two directions share one space rather than two.
    }
    \note<5>{
        At query time only part of this runs, in the order numbered here: encode the
        measured spectrum, look up its nearest molecule latents in the index, run
        those candidates forward through their own encoders, decode each one's
        predicted spectrum, then rerank by comparing it against what we measured.
        Retrieval proposes, the forward model checks.
    }
\end{frame}

\section{Results}
\begin{frame}{Results -- Ablation}{Mass Sets the Bar}
    \footnotesize{ Held-out test split $\cdot$ $N = 13$ queries $\cdot$ gallery of $66$ EI compounds}
    \vskip 2em
    \centering
    \begin{tabular}{ l r }
      \hline
      Ranking signal & MRR \\
      \hline
      True parent mass (oracle)      & $0.923$ \\
      Spectrum-implied parent mass   & $0.462$ \\
      Mass mask $+$ cosine           & $0.498$ \\
      Learned embedding              & $0.112$ \\
      \hline
    \end{tabular}
    \vskip 1.6em
    {\footnotesize $\Rightarrow$ uncontrolled retrieval scores measure
    \textbf{mass agreement}, not chemistry}
\end{frame}
\note{
    I now turn to the results.
    I start with the observation that determines how every other number has to be read.

    With a gallery of only 66 compounds, parent mass almost identifies the molecule on its own.
    An oracle that ranks candidates by their true mass attains a mean reciprocal rank of 0.92.
    Even a mass estimate read off the heaviest peak attains 0.46.

    The learned embedding, scored on the full gallery, attains 0.11.

    So none of these numbers beat mass.

    The reranker combines a mass mask with the forward model.
    Drop the forward model and keep just the mask --- still 0.50.

    So the question is not how high the retrieval score is.
    It is whether anything remains once mass is controlled for.
    We will see that on the next slide.
}

\begin{frame}{Results -- Mass-Controlled Retrieval}
    \centering
    \figorpending{width=0.72\linewidth}{../shared/figures/mass_controlled_mrr.pdf}
    \vskip 0.2em
    {\footnotesize
    $\pm5$\,Da: $0.551$ vs.\ chance $0.503$ \quad
    ($\Delta\,{+}0.048$, mean pool $5.4$) \\
    $\pm20$\,Da: $0.219$ vs.\ chance $0.206$ \quad
    ($\Delta\,{+}0.013$, mean pool $18.8$) \\[0.3em]
    Inside a window mass is ${\approx}$constant $\Rightarrow$ the margin is structure the mass cannot explain.}
\end{frame}
\note{
    Here mass is held fixed.
    I restrict the ranking to candidates within a window around the query's true mass.
    Plus or minus 5 Dalton, and plus or minus 20.
    Within such a window every candidate has essentially the same mass.
    So mass can no longer account for the ranking.

    I measure against the analytic chance level for a pool of that size.
    In the narrow window the learned retrieval attains 0.551, against 0.503.
    In the wide window, 0.219 against 0.206.
    That is a margin of roughly five and one percentage points.

    What is important is that they are positive on molecules seen neither in training nor in model selection.
    And they are the only part of the evaluation that mass cannot explain.
}

\begin{frame}{Results -- The Forward Model Is Molecule-Specific}
    \centering
    \begin{adjustbox}{max totalsize={\textwidth}{0.62\textheight},center}
        \input{../shared/figures/recon_benzene.tikz.tex}
    \end{adjustbox}
\end{frame}
\note{
    \tiny
    One more result, and this one concerns the forward model rather than retrieval.

    Unconstrained spectrum decoders have a well-known failure mode: they predict roughly the dataset-average spectrum for every input, and a reranker on top only appears to help, because the help comes from the mass filter.
    Mine cannot do that, by construction --- every predicted intensity is scattered from fragments derived for the input molecule.

    The figure shows benzene, measured upward and predicted mirrored downward.
    Predicted peaks fall at 78, the molecular ion, 77 for hydrogen loss, and 51 for a fragment, and all three appear in the measurement.
    A fourth predicted peak sits at 26, which the rules derive but the measurement does not show.
    Vice versa, the measured spectrum has a peak at 39, which the rules do not derive and the model does not predict.

    Both misses have an explanation, and they differ in kind.
    The peak at 26 may well be right and simply unobservable: EI spectra are usually not recorded below about mass-to-charge 35.
    The peak at 39 is the opposite case --- a real peak my rules do not reach.

    Corpus-wide, fifty-five percent of the intensity the rules cannot explain sits on even-electron ions, the signature of hydrogen transfer rather than simple cleavage.

    The rules account for about forty-two percent of measured peak intensity.
    So the rules are a ceiling: the model can only work with the part of the spectrum they reach, and no amount of training moves that bound.
}


\section{Conclusions}
\begin{frame}{Conclusions}
    \textbf{Contributions}
    \begin{itemize}
        \item \textbf{Fragment-grounded forward model}: intensities scattered
              from the molecule's own derived fragments $\Rightarrow$
              molecule-specific \emph{by construction}
        \item \textbf{Mass-controlled evaluation}: separates learned structure
              from parent-mass filtering
    \end{itemize}
    \vskip 1em
    {\footnotesize \textbf{Supporting work}: bidirectional framework
    ($f: \mathcal{M} \!\to\! \mathcal{S}$, $f^{-1}: \mathcal{S} \!\to\! \mathcal{M}$)
    $+$ rule-extension DSL}

\end{frame}
\note{
    To summarize.
    There are two contributions I would defend.

    First, the forward model is fragment-grounded.
    Every predicted intensity is scattered from the molecule's own derived fragments.
    So it is molecule-specific by construction --- a design property, not an empirical accident.

    Second, and more importantly, the evaluation.
    Mass is so strong a signal here that most retrieval figures amount to mass filtering.
    The mass-controlled protocol separates the two.

    Around those sit the bidirectional framework and the rule-extension DSL.

    %Two things before questions.

    %First, the pieces were tested on their own, not only end to end.
    %Against same-formula decoys, where parent mass is useless by construction, scoring the derived fragment masses alone puts the true molecule first in half of a hundred and thirty-two targets, against a chance level of twenty-three percent.
    %So the fragmentation side carries structural information independently of the model that consumes it.

    %Second, the bound: the rules explain about forty percent of measured intensity, so the retrieval numbers should be read against that ceiling, not against a perfect model.
}

\begin{frame}{Outlook \& Literature comparison}
    {Learn to \emph{Generate} Fragmentation}
        \centering
        \begin{adjustbox}{max totalsize={\linewidth}{0.72\textheight},center}
            \reportmetrics\input{../shared/figures/generative-future.tikz.tex}
        \end{adjustbox}
        % ICEBERG: credited in the spoken note and marked in the corner.
        \source{Goldman2024}
\end{frame}
\note{
    \small
    A structural design premise of this thesis: the model encodes precomputed fragmentation graphs.
    It does not learn fragmentation itself.

    The principal inspiration for this is ICEBERG, by Goldman and colleagues.
    ICEBERG divides the task in two.
    A Generate module proposes likely fragmentation events.
    A Score module assigns the intensities.

    Viewed through that lens, my model already provides the Score side --- the lower half of the figure.
    It embeds molecules, fragments and spectra into a shared latent space, and produces both the forward prediction and the inverse retrieval.

    What is missing is the Generate module upstream, the component that learns to propose fragmentation.
    The symbolic fragmentation graphs could serve as proxy supervision for training exactly that.

    The two would then be trained end-to-end.
    The model would learn not only how to use fragments, but which fragmentation structures to construct in the first place.
}

\begin{frame}{References}
    \printbibliography[heading=none]
\end{frame}
\note{
    These are the sources and references used in this presentation.
}

\begin{frame}{Discussion}
    \center{Questions?}
\end{frame}
\note{
    That concludes the presentation of my thesis.

    Thank you for your attention.

    I would be happy to take your questions.
}


% =====================================================================
% Backup material: the appendix lives in its own file, see appendix.tex.
% It opens with \recordmainframetotal (ending the talk proper's count) and
% closes with \recordsuppframetotal (the backup slides' own count).
% =====================================================================
\input{appendix}

\end{document}
